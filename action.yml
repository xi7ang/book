name: 'Book Notify'
description: 'Notification action for book repository'
author: 'mswnlz'
inputs:
  message:
    description: 'The message to print'
    required: false
    default: 'Hello from Book Action!'
runs:
  using: 'node20'
  main: 'index.js'
